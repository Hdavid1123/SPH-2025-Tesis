#pragma once
#include <stdexcept>

namespace kernels {

// Dimensiones soportadas en todos los kernels SPH
enum Dimension {
    ONE_D   = 1,
    TWO_D   = 2,
    THREE_D = 3
};

} // namespace kernels
